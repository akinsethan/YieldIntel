import type { Metadata } from "next";
import { RILASimulatorPage } from "@/components/pages/RILASimulatorPage";

export const metadata: Metadata = { title: "RILA Simulator — YieldIntel" };
export default function RilaPage() { return <RILASimulatorPage />; }
